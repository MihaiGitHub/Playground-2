import "./App.css";
import Parent from "./components/Parent";

// Create Modal with Increment button to increment a counter.
// Create a Reset Button in the parent component that resets counter to 0

function App() {
  return (
    <div className="App">
      <Parent />
    </div>
  );
}

export default App;
